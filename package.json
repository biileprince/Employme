{
  "name": "employme-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "cd server && npm install --include=dev && npx prisma generate && npm run build && npm prune --production",
    "start": "cd server && npx prisma migrate deploy && npm start"
  }
}
